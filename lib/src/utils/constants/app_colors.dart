import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(122, 93, 62, 1);
  static const textPrimary = Color.fromRGBO(33, 34, 41, 1);
  static const textSecondary = Color.fromRGBO(148, 150, 170, 1);
  static const textOnBackground = Color.fromRGBO(73, 74, 87, 1);

  // Standard Colors
  static const white = Colors.white;
  static const black = Colors.black;
  static const transparent = Colors.transparent;
}
